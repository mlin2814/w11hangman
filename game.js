exports.gamejs = {
	wordList: ["javascript", "java", "python",	"ruby"]
// create a word list to use in main.js
};
// var 
// 	var randomWord = Math.floor((Math.random()*(wordList.length + 1)));
// 	var gameWord = wordList[randomWord];